<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  >
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name="org.dieschnittstelle.mobile.android.todo.viewmodel.DateItemApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true" >

        <activity
            android:name="org.dieschnittstelle.mobile.android.todo.DetailviewActivity"
            android:exported="true"
            android:label="DataAccess"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="org.dieschnittstelle.mobile.android.todo.OverviewActivity">

        </activity>
        <activity android:name="org.dieschnittstelle.mobile.android.todo.widgets.DatePickerActivity" />
        <activity android:name="org.dieschnittstelle.mobile.android.todo.LoginActivity" />


    </application>

</manifest>